name: publish
on:
  release:
    types: [published]
permissions:
  contents: read
  id-token: write   # required for OIDC on the calling job
jobs:
  publish:
    permissions:
      contents: read
      id-token: write
    uses: unabandoned/.github/.github/workflows/reusable-publish.yml@5218bc51c951148887c6a7322f553e50ad67821b # main
    with:
      install-for-publish: true
    secrets: inherit
